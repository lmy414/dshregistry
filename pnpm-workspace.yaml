# pnpm 11 settings(pnpm 10+ 起配置集中于此,package.json 的 "pnpm" 字段不再读取)
# overrides:强制 @sapphire/shapeshift ≥3.9.7——3.0.0 的 s.boolean.optional.default(false)
# 组合在字段缺失时必抛错,导致 Crawlee RequestQueue2 的 prolong/deleteRequestLock 永远失败、队列死循环。
overrides:
  '@sapphire/shapeshift': ^3.9.7
